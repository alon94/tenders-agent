import { DocPage } from '../lib/docPage';

export const dynamic = 'force-dynamic';
export const metadata = { title: 'הצהרת נגישות · שווה מכרזים' };

export default function AccessibilityPage() {
  return <DocPage slug="accessibility" />;
}
