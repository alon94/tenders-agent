import { DocPage } from '../lib/docPage';

export const dynamic = 'force-dynamic';
export const metadata = { title: 'תנאי שימוש · שווה מכרזים' };

export default function TermsPage() {
  return <DocPage slug="terms" />;
}
